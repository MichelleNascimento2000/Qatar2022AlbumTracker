import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Qatar2022AlbumTracker',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
