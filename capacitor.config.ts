import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.michellenasci.albumtracker2022',
  appName: 'Qatar 2022 Album Tracker',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
